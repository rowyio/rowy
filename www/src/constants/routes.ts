export enum routes {
  home = "/",
  auth = "/auth",
  signOut = "/signOut",

  table = "/table",
  tableWithId = "/table/:id",
  grid = "/grid",
  gridWithId = "/grid/:id",
  editor = "/editor",
}

export default routes;
